# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# Poetry
backend/dist/

# Env / secrets
.env
.env.*
!.env.example
# Firebase service-account credentials (e.g. <project-id>-firebase-adminsdk-<id>.json) --
# must never live in the repo at all (passed via env var instead), but this is a second
# layer of protection in case one ever ends up here anyway.
*firebase-adminsdk*.json

# Node
node_modules/
dist/
.vite/

# Flutter/Dart
driver-app/.dart_tool/
driver-app/build/
driver-app/.flutter-plugins
driver-app/.flutter-plugins-dependencies
*.apk
*.aab

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs
*.log
